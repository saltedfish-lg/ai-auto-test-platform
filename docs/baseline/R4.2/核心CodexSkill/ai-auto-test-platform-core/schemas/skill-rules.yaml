metadata:
  release_id: PDBR-2026.08.07-R4.2
  version: 1.4.2-auth-contract-completion
  status: P1_AUTHENTICATION_CONTRACT_COMPLETED
  code_readiness: READY_FOR_P1_IMPLEMENTATION
  parent_release_id: PDBR-2026.08.06-R4.1
  last_modified_at: '2026-08-07T00:00:00+08:00'
must_read:
- ../../../编码冻结基线/RELEASE/platform_design_baseline_release.yaml
- ../../../编码冻结基线/SYSTEM_DESIGN.yaml
- ../../../编码冻结基线/OPENAPI/openapi.yaml
- ../../../编码冻结基线/DATABASE_DDL/database-schema.yaml
- ../../../编码冻结基线/AUTHENTICATION_CONTRACT/authentication-contract.yaml
invariants:
  technical_status: FROZEN
  write_api:
  - IDEMPOTENCY_KEY_REQUIRED
  - EXPECTED_VERSION_REQUIRED
  - RBAC_DATA_SCOPE_STATE_GUARD
  database:
  - FORMAL_RELATION_STATE_PERMISSION_UNIQUE_PHYSICALIZED
  - EXTENSION_JSON_NON_CONTRACT_ONLY
  events:
  - SEQUENCE_REQUIRED
  - PER_AGGREGATE_ORDER
  - SECRETS_FORBIDDEN
  review:
  - SUBMITTER_NE_APPROVER
  - APPROVE_REJECT_PUBLISH_SEPARATE_PERMISSIONS
  acceptance:
  - SPECIFIED_NE_PASSED
  - PASS_REQUIRES_EVIDENCE
  nfr:
  - PRD-NFR-001
  - PRD-NFR-002
  - PRD-NFR-003
  credential_revision_identity: credential_revision_id; test_account_id+revision_no unique
  technical_alert_endpoint_identity: technical_alert_endpoint_id; signature_config_ref is mutable reference
  run_task_initial_final_result: UNKNOWN
  code_readiness: READY_FOR_P1_IMPLEMENTATION
  implementation_release_readiness: NOT_EVALUATED_IMPLEMENTATION_NOT_PRESENT
prohibited:
- USE_R2_AS_CURRENT
- GENERIC_UNTYPED_API_PAYLOAD
- JSON_AS_FORMAL_RELATION_OR_STATE
- APPROVER_EQUALS_SUBMITTER
- PASS_WITHOUT_EVIDENCE
- 不得使用project_id作为CredentialRevision主键
- 不得使用signature_config_ref作为TechnicalAlertEndpoint主键
- 不得把真实平台验收证据作为开始编码的循环前置条件
- 不得在未执行MySQL 8.4门禁时宣称PASS
- 不得自创认证Operation、DTO、状态、Token、Cookie或密码政策
- 不得写死、回显、记录或提交admin初始密码
- 不得把Refresh Token原值写入数据库、JSON、日志或仓库
- 不得把Permission写入JWT作为长期授权事实
- 不得按username绕过正式RBAC Mapping
authority_model:
  model_id: AUTHORITY-MODEL-R4.2-001
  responsibilities:
  - authority: RELEASE_AND_MANIFEST
    scope: 当前发布身份、成员资格、版本、状态和哈希；不得改写产品业务语义。
  - authority: SIX_CORE_YAML_FACTS
    scope: 产品范围、角色场景、对象规则、权限并发、AI/Runner和安全验收业务语义。
  - authority: SYSTEM_DESIGN_AND_ENGINEERING_CONTRACTS
    scope: 技术实现和物理契约；必须服从六份核心YAML，同一工程契约内部以标准格式文件为权威。
  - authority: ADR
    scope: 决策及理由；只有同步受影响核心事实与工程契约后才成为当前实施依据。
  - authority: AGENTS_AND_SKILL
    scope: Codex执行流程、修改边界和门禁；不得自行改变产品或工程契约。
  - authority: NAVIGATION_AND_PROJECTIONS
    scope: Markdown导航、DOCX和图形为非权威阅读投影。
  conflict_resolution:
    product_fact_vs_engineering_contract: 停止实现并以六份核心YAML为业务语义，修复工程契约后继续。
    adr_vs_old_yaml: ADR必须先同步核心YAML和工程契约；未同步时不得仅凭ADR覆盖。
    release_vs_business_fact: Release只决定成员和版本，不得覆盖业务事实。
gates:
- gate_id: MYSQL84_EMPTY_DATABASE_EXECUTION
  status: PASS
  blocking_scope: DATABASE_MODULE_FORMAL_MERGE
  blocks_code_initialization: false
- gate_id: REAL_ACCEPTANCE_EVIDENCE
  status: NOT_EVALUATED_IMPLEMENTATION_NOT_PRESENT
  blocking_scope: IMPLEMENTATION_RELEASE_READINESS
  blocks_code_initialization: false
